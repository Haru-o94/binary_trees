#include "binary_trees.h"

/**
 * heap_to_sorted_array - sorted array
 *
 * @heap: pointer
 * @size: number
 * Return: array sorted
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	return (heap);
}