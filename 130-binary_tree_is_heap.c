#include "binary_trees.h"

/**
 * binary_tree_is_heap - tree is heapp
 * @tree: Pointer tree's node
 * Return: 1 if the tree is a heap
 */
int binary_tree_is_heap(const binary_tree_t *tree)
{
	return (tree);
}
